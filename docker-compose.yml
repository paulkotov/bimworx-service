services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bimworx-service
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
