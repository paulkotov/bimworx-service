services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bimworx-service
    ports:
      - "${PORT:-2504}:2504"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 2504
    restart: unless-stopped
